apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlserver-pvc
  namespace: oficina
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sqlserver-gp3
  resources:
    requests:
      storage: 2Gi
